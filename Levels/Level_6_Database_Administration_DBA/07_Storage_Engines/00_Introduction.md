- In MariaDB and MySQL, the Storage Engine is the software component that handles the actual "reading" and "writing" of data to the disk.

While the SQL language stays the same, changing the engine changes how the database behaves—kind of like putting a racing engine versus a truck engine into the same car body.
