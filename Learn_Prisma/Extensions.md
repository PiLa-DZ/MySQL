```Bash
npx prisma format

# 🚀 One Last Cool Thing
# Since you are in the terminal, run this command:
# It will open a browser window at localhost:5555. You can add, edit, and delete students and courses using a beautiful interface instead of Postman!
npx prisma studio
```
