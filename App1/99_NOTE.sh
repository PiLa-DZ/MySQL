- Database Administration (DBA):

- Database Provisioning: "Setup Database to run first time"

- Database Design
    1. Conceptual Design
        - Entities:(User, Product, Order).
        - Attributes:(Name, Email, Price).
        - ERD (Entity Relationship Diagram).
    2. Logical Design
        - Relationships: (One-to-One, One-to-Many, Many-to-Many).
        - Normalization: (1NF, 2NF, 3NF).
        - Primary Keys & Foreign Keys
    3. Physical Design
        - Data Types: (INT, VARCHAR, DECIMAL).
        - Indexing: For fast search
        - Constraints: (NOT NULL, UNIQUE, CHECK).
        - Storage Engine: Choosing Storage Engine like (مثل InnoDB في MySQL).
        - "NOTE: For Arabic" --> Character Set & Collation: (utf8mb4).

- Ask 
    - [x] Rilationship: How to add one user to many grops  --> JUNCTION Table & GROUP_CONCAT
    - [x] Database Administration: Why we can DROP DATABASE very easy --> Learn Database Administration (DBA)
    - [x] Rilationship (One-to-One, One-to-Many, Many-to-Many) --> Database Design (Logical Design)
    - [x] sql json --> JSON With MySQL (JSON_OBJECT, JSON_ARRAY, JSON_EXTRACT)
    - [x] Send Output From sql to file --> (TEE Path, NOTEE)
    - [x] Normalization: (1NF, 2NF, 3NF)

    - JSON With MySQL (JSON_OBJECT, JSON_ARRAY, JSON_EXTRACT)


